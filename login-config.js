const ACCESS_TOKEN_EXP = 5 * 60; // 5 minutes
const REFRESH_TOKEN_EXP = 7 * 86400; // 7 days
const JWT_NAME = "jwt"; // dev setting, change in production
const SECURE_FLAG = false; // dev setting, CHANGE IN PRODUCTION
export { ACCESS_TOKEN_EXP, REFRESH_TOKEN_EXP, JWT_NAME, SECURE_FLAG };
